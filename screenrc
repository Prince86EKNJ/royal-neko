defscrollback 1000000
hardstatus off

bind -k ku focus up
bind -k kd focus down
bind -k kl focus left
bind -k kr focus right

bind S eval 'split' 'focus down' 'screen'
bind s eval 'split' 'focus down'
bind | eval 'split -v' 'focus right' 'screen'
bind \ eval 'split -v' 'focus right'
